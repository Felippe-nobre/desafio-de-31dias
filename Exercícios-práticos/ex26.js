let numeros = [1, 2, 13, 4, 15, 6, 7, 18, 9, 10];
let m = 10;

for (let i = 0; i < numeros.length; i++) {
    if (numeros[i] > 10) {
        console.log(`Índice ${i}: número maior que 10 é o ${numeros[i]}`);
    }
}
