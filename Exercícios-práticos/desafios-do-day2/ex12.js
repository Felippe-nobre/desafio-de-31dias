let caixaEletronico = 1
switch (caixaEletronico) {
    case 1:
       let sacar = 500;
        console.log(`Voce sacou R$ ${sacar}`);
        break;
    case 2:
        let depositar = 100;
        console.log(`Voce depositou R$ ${depositar}`);
        break;
    case 3:
        let saldo = console.log(`Seu saldo é de R$ ${saldo}`);
        break;    
    default:
        console.log("Opcão invalida");
        break;
}